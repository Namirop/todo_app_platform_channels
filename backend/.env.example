# Database connection
# Option 1: Use Docker (docker-compose up -d)
# Option 2: Create a Railway PostgreSQL database (https://railway.app)
DATABASE_URL=postgresql://todoapp:dev_password@localhost:5432/todoapp
# Generate random secret keys (https://generate-secret.vercel.app/32)
JWT_SECRET=your-random-secret-key-here
JWT_REFRESH_SECRET=your-random-refresh-secret-key-here
PORT=3000